main
	var a, b, c;
	{
		let a <- 3;
		let b <- call InputNum();
		if b > a then
			let a <- c + 1;
		else
			call OutputNum(c);
			let b <- a + 1;
		fi;
		call OutputNum(a);
		call OutputNum(b);
		call OutputNum(c);
	}.
