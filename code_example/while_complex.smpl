main
	var a, b, c;
	{
		let a <- 3;
		let b <- call InputNum();
		while a > b do
			let a <- a - b + c;
			let b <- b + 1 + c;
			call OutputNum(b);
			while b > c do
				let a <- 1;
				let b <- 2;
			od;
		od;
		call OutputNum(c);
	}.
